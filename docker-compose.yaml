# docker-compose.yml
services:
  broke-server:
    restart: always
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - data:/data
    environment:
      - DATA_PATH=/data

volumes:
  data:
